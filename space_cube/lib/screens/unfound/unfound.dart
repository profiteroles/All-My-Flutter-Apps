import 'package:flutter/material.dart';

class UnfoundPage extends StatelessWidget {
  const UnfoundPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: const Text('Not Found Page'),
        ),
      );
}
